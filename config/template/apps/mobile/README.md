# Fitted Mobile (NativeScript + Vue)
Initialize with:
  npx @nativescript/cli@latest create . --template @nativescript/template-blank-vue
Then replace app files with ./app from this folder.
Run:
  ns run ios --hmr   # or android
